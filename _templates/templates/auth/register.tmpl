{{ define "title"}}Sign up{{ end }}

{{ define "content" }}
    <div class="container">
        <h1>Sign up</h1>
        <form method="POST" action="">
            {{ template "form" .form }}
        </form>
    </div>
{{ end }}
